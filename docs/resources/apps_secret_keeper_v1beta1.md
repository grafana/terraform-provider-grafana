---
page_title: "grafana_apps_secret_keeper_v1beta1 Resource - terraform-provider-grafana"
subcategory: "Grafana Enterprise"
description: |-
  Manages a Secrets Management keeper.
---

# grafana_apps_secret_keeper_v1beta1 (Resource)

Manages a Secrets Management keeper.

Note: the namespace is inferred from the provider stack ID (or org ID for OSS).
Use `grafana_apps_secret_keeper_activation_v1beta1` to select the active keeper; deleting that resource activates the `system` keeper.

## Example Usage

```terraform
resource "grafana_apps_secret_keeper_v1beta1" "aws_production" {
  metadata {
    uid = "aws-production"
  }
  spec {
    description = "AWS Production"

    aws {
      region = "us-east-1"

      assume_role {
        assume_role_arn = "arn:aws:iam::123456789012:role/GrafanaSecretsAccess"
        external_id     = "grafana-unique-external-id"
      }
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `metadata` (Block) The metadata of the resource. (see [below for nested schema](#nestedblock--metadata))
- `spec` (Block) The spec of the keeper. (see [below for nested schema](#nestedblock--spec))

### Optional

- `options` (Block) Options for applying the resource. (see [below for nested schema](#nestedblock--options))

### Read-Only

- `id` (String) The ID of the resource derived from UUID.

<a id="nestedblock--metadata"></a>
### Nested Schema for `metadata`

Required:

- `uid` (String) The unique identifier of the resource. Must be DNS-1123 compatible.

Optional:

- `folder_uid` (String) The UID of the folder to save the resource in.

Read-Only:

- `annotations` (Map of String) Annotations of the resource.
- `uuid` (String) The globally unique identifier of a resource, used by the API for tracking.
- `url` (String) The full URL of the resource.
- `version` (String) The version of the resource.

<a id="nestedblock--spec"></a>
### Nested Schema for `spec`

Optional:

- `description` (String) Keeper description.
- `aws` (Block) AWS Secrets Manager configuration. (see [below for nested schema](#nestedblock--spec--aws))

<a id="nestedblock--spec--aws"></a>
### Nested Schema for `spec.aws`

Required:

- `region` (String) AWS region.
- `assume_role` (Block) IAM role assumption configuration. (see [below for nested schema](#nestedblock--spec--aws--assume_role))

<a id="nestedblock--spec--aws--assume_role"></a>
### Nested Schema for `spec.aws.assume_role`

Required:

- `assume_role_arn` (String) Assume role ARN.
- `external_id` (String) Assume role external ID.

<a id="nestedblock--options"></a>
### Nested Schema for `options`

Optional:

- `overwrite` (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.

## Import

Import is supported using the following syntax:

```shell
terraform import grafana_apps_secret_keeper_v1beta1.name "{{ name }}"
```
